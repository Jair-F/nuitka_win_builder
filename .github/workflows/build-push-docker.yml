name: Build + Publish

on:
  push:
    tags:
      - '*'

jobs:
  build-push-container:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: jairf
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
      - name: build and push to DockerHub
        uses: docker/build-push-action@v5.1.0
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: linux/amd64
          tags: jairf/nuitka_win_builder:latest # jairf/nuitka_win_builder:${{ github.ref_name }}
#   build-push-container-offline:
#     runs-on: ubuntu-24.04
#     needs: build-push-container
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Docker Buildx
#         uses: docker/setup-buildx-action@v3
#       - name: Login to DockerHub
#         uses: docker/login-action@v3
#         with:
#           username: jairf
#           password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
#       - name: build and push to DockerHub
#         uses: docker/build-push-action@v5.1.0
#         with:
#           context: .
#           file: Dockerfile.offline
#           push: true
#           platforms: linux/amd64
#           tags: jairf/nuitka_win_builder:${{ github.ref_name }}_offline
#           build-args: BUILDER_VERSION=${{ github.ref_name }}
